{
  "name": "eth-tech-tree-backend",
  "version": "0.0.1",
  "description": "backend for managing users and challenges for the ETH Tech Tree",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/server"
    ]
  },
  "scripts": {
    "build": "yarn workspace @ett-backend/server build",
    "start": "yarn workspace @ett-backend/server start",
    "dev": "yarn workspace @ett-backend/server dev",
    "install:challenges": "yarn workspace @ett-backend/server install:challenges",
  },
  "packageManager": "yarn@3.2.3",
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3"
  }
}
